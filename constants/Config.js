export const isDevelopmentMode = true; // Reinstall app (to clear redux persist) if you change this and want to see effects

export const STRING_LENGTH_SHORT = 25;
export const STRING_LENGTH_MEDIUM = 100;
export const STRING_LENGTH_LONG = 500;

const Config = {
	isDevelopmentMode,
	STRING_LENGTH_SHORT,
	STRING_LENGTH_MEDIUM,
	STRING_LENGTH_LONG,
}

export default Config